{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user in the ChessVerse application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user."
        },
        "email": {
          "type": "string",
          "description": "User's email address.",
          "format": "email"
        },
        "username": {
          "type": "string",
          "description": "User's display name."
        },
        "eloRating": {
          "type": "number",
          "description": "The user's ELO rating."
        },
        "completedGames": {
          "type": "number",
          "description": "Number of games completed by the user."
        }
      },
      "required": [
        "id",
        "email",
        "username"
      ]
    },
    "Game": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Game",
      "type": "object",
      "description": "Represents a chess game.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the game."
        },
        "player1Id": {
          "type": "string",
          "description": "Reference to User 1. (Relationship: User 1:N Game)"
        },
        "player2Id": {
          "type": "string",
          "description": "Reference to User 2. (Relationship: User 1:N Game)"
        },
        "startTime": {
          "type": "string",
          "description": "Timestamp indicating the start time of the game.",
          "format": "date-time"
        },
        "endTime": {
          "type": "string",
          "description": "Timestamp indicating the end time of the game.",
          "format": "date-time"
        },
        "moves": {
          "type": "array",
          "description": "List of chess moves in the game.",
          "items": {
            "type": "string"
          }
        },
        "winnerId": {
          "type": "string",
          "description": "Reference to User who won the game. Null if the game is still ongoing or it was a draw. (Relationship: User 1:N Game)"
        },
        "fen": {
          "type": "string",
          "description": "The board state in Forsyth-Edwards Notation."
        },
        "turn": {
          "type": "string",
          "enum": [
            "w",
            "b"
          ],
          "description": "The current player's turn ('w' for white, 'b' for black)."
        },
        "player1Clock": {
          "type": "number",
          "description": "Remaining time for player 1 in seconds."
        },
        "player2Clock": {
          "type": "number",
          "description": "Remaining time for player 2 in seconds."
        },
        "lastMove": {
          "type": "string",
          "description": "The last move made in Standard Algebraic Notation (SAN)."
        }
      },
      "required": [
        "id",
        "player1Id",
        "startTime",
        "fen",
        "turn"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profiles. Access control is based on path-based ownership.  A user can only access their own document.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user."
            }
          ]
        }
      },
      {
        "path": "/games/{gameId}",
        "definition": {
          "entityName": "Game",
          "schema": {
            "$ref": "#/backend/entities/Game"
          },
          "description": "Stores game state. It is publicly readable but can only be modified by players in the game.",
          "params": [
            {
              "name": "gameId",
              "description": "The unique identifier for the game."
            }
          ]
        }
      },
      {
        "path": "/games/{gameId}/moves/{moveId}",
        "definition": {
          "entityName": "Move",
          "schema": {
            "$ref": "#/backend/entities/Move"
          },
          "description": "Stores the history of moves for a game. This is append-only.",
          "params": [
            {
              "name": "gameId",
              "description": "The unique identifier for the game."
            },
            {
              "name": "moveId",
              "description": "The unique identifier for the move."
            }
          ]
        }
      }
    ]
  }
}